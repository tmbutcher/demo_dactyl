manifest:
  
  defaults:
    revision: main
  
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    
    - name: urob
      url-base: https://github.com/urob
  
  projects:
    - name: zmk
      remote: zmkfirmware
      import: app/west.yml

    # Modules
    - name: zmk-helpers
      remote: urob
      # revision: main
    - name: zmk-unicode
      remote: urob
      # revision: main
    - name: zmk-auto-layer
      remote: urob
      # revision: main

  self:
    path: config
